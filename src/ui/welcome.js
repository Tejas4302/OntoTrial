const brand='<a class="entry-brand" href="#home"><img src="/assets/favicon.svg" width="36" height="36" alt="">OntoTrail<span>●</span></a>';
let session=null;
async function auth(method='GET',body){
  const response=await fetch('/api/auth',{method,headers:body?{'Content-Type':'application/json'}:{},body:body?JSON.stringify(body):undefined,credentials:'same-origin'});
  const data=await response.json().catch(()=>({}));
  if(!response.ok)throw Error(data.error||'Authentication failed.');
  return data;
}
function applySession(value){session=value||null;const ws=document.querySelector('.sidebar-bottom strong');const sub=document.querySelector('.sidebar-bottom span');const avatar=document.querySelector('.workspace-avatar');if(ws)ws.textContent=session?.tenant||'Protected workspace';if(sub)sub.textContent=session?.role==='admin'?'OntoTrail administrator':'Client workspace';if(avatar)avatar.textContent=session?.role==='admin'?'OA':'CC';}
export function homePage(){return `<header class="entry-nav">${brand}<nav aria-label="Home navigation"><a href="#features">Platform</a><a href="#login">Client login</a><a class="primary" href="#login">Open secure workspace →</a></nav></header><main class="landing" id="entry-main" tabindex="-1"><section class="hero"><div class="hero-copy"><p class="entry-kicker">CONNECTED SUPPLY CHAIN INTELLIGENCE</p><h1>Every disruption.<br>Every dependency.<br><em>A clearer decision.</em></h1><p class="hero-description">OntoTrail connects supply-chain relationships with conversational analytics so planning teams can trace impact, compare recovery options and make evidence-backed decisions.</p><div class="hero-actions"><a class="primary" href="#login">Client login →</a><a class="secondary" href="#features">Explore the platform</a></div><p class="hero-caption"><span>●</span> Secure tenant workspace · Governed analytics · Snowflake powered</p></div><div class="hero-visual secure-preview" aria-label="Secure workspace illustration"><div class="visual-top"><span class="mini-brand">◉ OntoTrail</span><span class="mini-pill">SECURE WORKSPACE</span></div><div class="preview-heading"><span>Client data stays behind authentication.</span><small>Tenant-scoped access to analytics and planning workflows</small></div><div class="security-grid"><div><strong>Tenant isolation</strong><small>Each client enters a dedicated workspace.</small></div><div><strong>Authenticated access</strong><small>No operational data is shown before sign-in.</small></div><div><strong>Governed analytics</strong><small>Questions are grounded in the configured Snowflake semantic view.</small></div><div><strong>Auditable answers</strong><small>Generated SQL remains available for traceability.</small></div></div></div></section><section class="landing-features" id="features"><div class="section-intro"><p class="entry-kicker">FROM SIGNAL TO ACTION</p><h2>Less searching.<br>More understanding.</h2><p>A connected workspace for supply-chain planning, impact analysis and conversational analytics.</p></div><article><span class="feature-number">01 / VISIBILITY</span><h3>Know what is at risk.</h3><p>Connect suppliers, products, plants and customer orders in a single planning view.</p></article><article><span class="feature-number">02 / TRACEABILITY</span><h3>Understand why.</h3><p>Trace analytical answers back to governed data and generated SQL.</p></article><article><span class="feature-number">03 / SCENARIOS</span><h3>Compare before you act.</h3><p>Evaluate disruption and recovery scenarios without exposing client data publicly.</p></article></section><section class="landing-cta"><div><p class="entry-kicker">PROTECTED CLIENT ACCESS</p><h2>Your workspace begins after authentication.</h2></div><a class="primary" href="#login">Log in to OntoTrail →</a></section></main><footer class="entry-footer"><span>© ${new Date().getFullYear()} OntoTrail</span><span>Supply chain ontology + conversational analytics</span><a href="#login">Client login →</a></footer>`;}
export function authPage(){return `<div class="auth-layout"><aside class="auth-story">${brand}<div class="auth-story-copy"><p class="entry-kicker">PROTECTED CLIENT WORKSPACE</p><h1>One workspace.<br>Every dependency.<br><em>Grounded answers.</em></h1><p>Sign in to access your organization’s supply-chain workspace and governed analytics.</p><div class="auth-proof"><span>01</span><div><strong>Tenant-aware access</strong><small>Client workspaces are separated by authenticated identity.</small></div></div><div class="auth-proof"><span>02</span><div><strong>Protected analytics</strong><small>Cortex requests are available only to authenticated sessions.</small></div></div><div class="auth-proof"><span>03</span><div><strong>Snowflake grounded</strong><small>Answers use the configured semantic view and retain SQL traceability.</small></div></div></div><small class="auth-story-footer">Authorized access only</small></aside><main class="auth-main" id="entry-main" tabindex="-1"><a class="back-home" href="#home">← Back to home</a><div class="auth-card"><span class="auth-tag">CLIENT ACCESS</span><h1>Sign in to OntoTrail.</h1><p class="auth-subtitle">Use the credentials provided to your organization.</p><form id="entry-auth" autocomplete="off"><label class="auth-field">Work email<input name="email" type="email" required maxlength="254" autocomplete="username"></label><label class="auth-field" for="entry-password">Password</label><div class="password-wrap"><input id="entry-password" name="password" type="password" required maxlength="128" autocomplete="current-password"><button type="button" data-entry="password" aria-label="Show password">Show</button></div><p id="auth-message" role="status" aria-live="polite"></p><button class="primary auth-submit">Enter secure workspace →</button></form><p class="auth-disclosure">Workspace data is available only after successful authentication.</p></div><p class="auth-bottom">Connect the dots. Trace the answers.</p></main></div>`;}
export function initWelcome(){
  const host=document.createElement('div');host.id='entry-root';document.body.append(host);const shell=document.querySelector('#shell');let ready=false;
  async function refreshSession(){try{const data=await auth();applySession(data.session);}catch{applySession(null);}ready=true;route();}
  function route(){if(!ready)return;let page=location.hash.slice(1)||'home';const publicPage=['home','login','features'].includes(page);if(!publicPage&&!session){history.replaceState(null,'',`${location.pathname}${location.search}#login`);page='login';}
    const active=publicPage||!session;host.hidden=!active;shell.hidden=active;document.querySelector('.skip-link').href=active?'#entry-main':'#main';
    if(!active){applySession(session);return;}host.innerHTML=page==='login'?authPage():homePage();document.title=`OntoTrail · ${page==='login'?'Client login':'Connected supply chain intelligence'}`;if(page==='features')host.querySelector('#features')?.scrollIntoView();else window.scrollTo(0,0);
  }
  host.addEventListener('click',event=>{const button=event.target.closest('[data-entry]');if(!button)return;if(button.dataset.entry==='password'){const input=host.querySelector('#entry-password');const show=input.type==='password';input.type=show?'text':'password';button.textContent=show?'Hide':'Show';}});
  host.addEventListener('submit',async event=>{if(event.target.id!=='entry-auth')return;event.preventDefault();if(!event.target.reportValidity())return;const message=host.querySelector('#auth-message');message.textContent='Signing in…';const fd=new FormData(event.target);try{const data=await auth('POST',{email:String(fd.get('email')||''),password:String(fd.get('password')||'')});applySession(data.session);location.hash='overview';}catch(err){message.textContent=err.message||'Sign-in failed.';}});
  document.addEventListener('click',async event=>{const button=event.target.closest('[data-auth-logout]');if(!button)return;event.preventDefault();button.disabled=true;try{await auth('DELETE');}catch{}applySession(null);location.hash='login';});
  window.addEventListener('hashchange',route);refreshSession();
}
